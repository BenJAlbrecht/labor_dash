[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "load-bls"
version = "0.1.0"
description = "Load and process U.S. Bureau of Labor Statistics flat files"
requires-python = ">=3.10"
dependencies = [
    "pandas",
    "requests",
]
